# Server Configuration
server.port=8080
server.servlet.context-path=/

# Application Info
spring.application.name=jhic-backend

# CORS Configuration Settings (Custom)
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# Logging Level
logging.level.root=INFO
logging.level.com.jhic.backend=DEBUG

# Spring JPA / Database placeholders (Uncomment when database is attached)
# spring.datasource.url=jdbc:postgresql://localhost:5432/jhic_db
# spring.datasource.username=postgres
# spring.datasource.password=postgres
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
